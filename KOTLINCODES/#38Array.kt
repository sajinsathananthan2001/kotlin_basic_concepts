fun main()
{
    //for integer
    var num = intArrayOf(1,2,3,4,5,6,7,8)

    //can use this way
    num[1] = 1

    //also we can use
    num.set(2,5)

    for(i in num)

        println(i)

    //for string 
    // for string we not learned correct concept so learn in feature 
}

/*
Also we hava (arrayOfNulls<"here you can specify the type">()) for example 'arrayOfNulls<String>()' */